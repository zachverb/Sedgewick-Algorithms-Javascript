import { All as sorts } from '../sorts';
export const TEST_NUM = 100; // the number of times to generate tests
export const TEST_SIZE = 1000; // the size of the test arrays
export const SORTS = sorts;